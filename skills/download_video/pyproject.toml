[project]
name = "autokiri-download-video"
version = "0.1.0"
description = "AutoKiri - 直播回放 (VOD) 与聊天室弹幕下载工具"
requires-python = ">=3.11"
dependencies = [
    "pyyaml",
    "python-dotenv",
]

[tool.uv]
package = false